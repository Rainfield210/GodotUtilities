using Godot;

namespace {{Namespace}};

partial class {{ClassName}} {
    private static PackedScene __Scene__ => field ??= GD.Load<PackedScene>("{{ResourcePath}}");

    public static {{ClassName}} Instantiate({{MethodSignature}}) {
        var scene = __Scene__.Instantiate<{{ClassName}}>();
        scene.{{MethodName}}({{MethodCallArgs}});
        return scene;
	}
}
